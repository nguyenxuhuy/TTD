$PBExportHeader$u_so.sru
forward
global type u_so from cl_so
end type
end forward

global type u_so from cl_so
end type
global u_so u_so

on u_so.create
call super::create
end on

on u_so.destroy
call super::destroy
end on

